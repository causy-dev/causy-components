import Button from "@/components/button/Button.vue";
import {Meta, Story} from "@storybook/blocks";
import Header from "./Header.vue";

<Meta title="Header" component={Header} />;
# Header
<div style={{display: "flex", gap: "10px"}}>
    <Header style={{maxWidth: "400px"}} size={"s"} >SMALL</Header>
</div>


<div style={{display: "flex", gap: "100px"}}>
    <Header style={{maxWidth: "400px"}} size={"m"} >MEDIUM</Header>
</div>

<div style={{display: "flex", gap: "100px", marginTop: "40px"}}>
    <Header style={{maxWidth: "400px"}} size={"l"} >LARGE</Header>
</div>
